# Databases
Come costruire un database di una biblioteca , usando database relazionali (SQL based), non relazionali (MongoDB con JSON) , basati a grafo (Neo4j con Cypher) e database ibrido (Mongo DB e PostgreSQL)
